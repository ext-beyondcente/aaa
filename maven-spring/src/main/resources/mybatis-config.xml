<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
  <configuration>
        <typeAliases>
                <package name="com.kuang.pojo"></package>
        </typeAliases>

        <mappers>
            <mapper class="com.kuang.dao.BookMapper"></mapper>
        </mappers>
  </configuration>































  
            <!-- properties配置数据库
            <properties resource="db.properties"/>

            
            <settings>
                <!-- 开启二级缓存 -->
                <setting name="cacheEnabled" value="true"/>
                <!-- 日志 -->
                <setting name="logImpl" value="LOG4J"/>
            
                <!-- 延迟加载 -->
                <setting name="lazyLoadingEnabled" value="true"/>
                <!-- 不要积极的查询关联对象 -->
                <setting name="aggressiveLazyLoading" value="false"/>
                <!-- 打印对象避免发送额外的SQL语句 ,指定的方法就会发送额外的SQL语句-->
                <setting name="lazyLoadTriggerMethods" value="equals"/>
                
            </settings>
            
            <!-- 别名 -->
            <typeAliases>
                <typeAlias type="com.mybaties.domain.User" alias="User"/>
                <typeAlias type="cn.mybaties.login.domain.User" alias="_User"/>
                <typeAlias type="com.mybaties.domain.Department" alias="Department"/>
                <!-- <package name="com.mybaties.domain"/> -->
            </typeAliases>
            
            <environments default="development">
                <environment id="development">
                    <!-- 事务管理器 -->
                    <transactionManager type="JDBC"></transactionManager>
                    <!-- 数据源 -->
                    <dataSource type="POOLED">
                        <property name="driver" value="${jdbc.driver}"/>
                        <property name="url" value="${jdbc.url}"/>
                        <property name="username" value="${jdbc.username}"/>
                        <property name="password" value="${jdbc.password}"/>
                    </dataSource>
                </environment>
            </environments>
            <!-- 关联Mapper文件 -->
            <mappers>
                <mapper resource="com\mybaties\mapper\UserMapper.xml"/>
                <mapper resource="com\mybaties\mapper\DepartmentMapper.xml"/>
            </mappers> -->
  